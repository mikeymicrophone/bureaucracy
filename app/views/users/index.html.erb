<h1>Listing users</h1>

<table>
  <tr>
  </tr>

<% @users.each do |user| %>
  <tr>
	<td><%= h user.login %></td>
    <td><%= link_to 'Show', user %></td>    
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New user', new_user_path %>